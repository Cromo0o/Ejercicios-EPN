//Using .c like a .txt only for the colors

return_type function_name(parameter_list){
    //body of the function
}

return_type: Especifica el tipo de dato a retornar. Se usa void si no se retorna ningun valor.

parameter_list: Lista de valores que tienen que ser pasados a la funcion. 
                Si no espera valores, puede dejarse vacio () o usar (void).

body of the function: El bloque de codigo que se ejecuta cuando se invoca la función; delimitado por llaves {}.



